FROM node:20-alpine

WORKDIR /app

# 复制应用源码
COPY apps/backend-main/ ./

# 删除源码自带的 .env（使用 Compose 注入的环境变量）
RUN rm -f .env .env.local .env.example

# 安装生产依赖
RUN npm install --omit=dev

ENV NODE_ENV=production

EXPOSE 10001

CMD ["node", "app.js"]
